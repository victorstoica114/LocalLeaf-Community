# LocalLeaf Community website

This folder contains the small public website for LocalLeaf Community. It is built with React and Vite and deployed through the repository's GitHub Pages workflow.

To run it locally:

```bash
npm install
npm run dev
```

To check the production build:

```bash
npm run lint
npm run build
```
