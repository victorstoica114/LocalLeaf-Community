diff --git a/node_modules/socket.io-client/lib/json.js b/node_modules/socket.io-client/lib/json.js
index 0820a52..e1885fb 100644
--- a/node_modules/socket.io-client/lib/json.js
+++ b/node_modules/socket.io-client/lib/json.js
@@ -301,8 +301,9 @@
   // JavaScript structure. The '{' operator is subject to a syntactic ambiguity
   // in JavaScript: it can begin a block or an object literal. We wrap the text
   // in parens to eliminate the ambiguity.
-
-          j = eval('(' + text + ')');
-
+          if (typeof global === 'undefined' || !global.JSON || !global.JSON.parse) {
+              throw new SyntaxError('Native JSON.parse is required');
+          }
+          j = global.JSON.parse(text);
   // In the optional fourth stage, we recursively walk the new structure, passing
   // each name/value pair to a reviver function for possible transformation.
diff --git a/node_modules/socket.io-client/lib/socket.js b/node_modules/socket.io-client/lib/socket.js
index 07c1c3f..862ebcd 100644
--- a/node_modules/socket.io-client/lib/socket.js
+++ b/node_modules/socket.io-client/lib/socket.js
@@ -152,11 +152,27 @@
       if (this.isXDomain()) {
         xhr.withCredentials = true;
       }
+      if (this.options['extraHeaders']) {
+        xhr.setDisableHeaderCheck(true);
+        Object.entries(this.options['extraHeaders']).forEach(([key, value]) => {
+          xhr.setRequestHeader(key, value);
+        });
+      }
       xhr.onreadystatechange = function () {
         if (xhr.readyState == 4) {
           xhr.onreadystatechange = empty;
 
           if (xhr.status == 200) {
+            // extract set-cookie headers
+            const matches = xhr.getAllResponseHeaders().match(/set-cookie:\s*([^\r\n]+)/gi);
+            matches && matches.forEach(function (header) {
+              const newCookie = header.split(':')[1].split(';')[0].trim();
+              const optCookie = self.options['extraHeaders']['Cookie'];
+              const mergedCookie = optCookie ? `${optCookie}; ${newCookie}` : newCookie;
+              self.options['extraHeaders'] = self.options['extraHeaders'] || {};
+              self.options['extraHeaders']['Cookie'] = mergedCookie;
+            });
+
             complete(xhr.responseText);
           } else if (xhr.status == 403) {
             self.onError(xhr.responseText);
@@ -229,7 +245,7 @@
         self.transport.ready(self, function () {
           self.connecting = true;
           self.publish('connecting', self.transport.name);
-          self.transport.open();
+          self.transport.open(self.options['extraHeaders']);
 
           if (self.options['connect timeout']) {
             self.connectTimeoutTimer = setTimeout(function () {
@@ -369,6 +385,12 @@
     ].join('/') + '/?disconnect=1';
 
     xhr.open('GET', uri, false);
+    if (this.options['extraHeaders']) {
+      xhr.setDisableHeaderCheck(true);
+      Object.entries(this.options['extraHeaders']).forEach(([key, value]) => {
+        xhr.setRequestHeader(key, value);
+      });
+    }
     xhr.send(null);
 
     // handle disconnection immediately
@@ -476,5 +498,14 @@
     this.connected = false;
     this.connecting = false;
     this.open = false;
-
+
+    // ACK callbacks and queued packets belong to the disconnected transport.
+    // Retaining them indefinitely leaks callbacks and potentially sensitive data.
+    for (var endpoint in this.namespaces) {
+      if (this.namespaces.hasOwnProperty(endpoint)) {
+        this.namespaces[endpoint].acks = {};
+      }
+    }
+    this.buffer = [];
+
     if (wasConnected || wasConnecting) {
@@ -579 +610 @@
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('./io')
diff --git a/node_modules/socket.io-client/lib/transports/websocket.js b/node_modules/socket.io-client/lib/transports/websocket.js
index 0dfa483..eab07d3 100644
--- a/node_modules/socket.io-client/lib/transports/websocket.js
+++ b/node_modules/socket.io-client/lib/transports/websocket.js
@@ -50,7 +50,7 @@
    * @api public
    */
 
-  WS.prototype.open = function () {
+  WS.prototype.open = function (extraHeaders) {
     var query = io.util.query(this.socket.options.query)
       , self = this
       , Socket
@@ -63,7 +63,11 @@
       Socket = global.MozWebSocket || global.WebSocket;
     }
 
-    this.websocket = new Socket(this.prepareUrl() + query);
+    this.websocket = new Socket(this.prepareUrl() + query, {
+      headers: extraHeaders || {},
+      maxPayload: 64 * 1024 * 1024,
+      maxBufferedChunks: 16 * 1024
+    });
 
     this.websocket.onopen = function () {
       self.onOpen();
diff --git a/node_modules/socket.io-client/lib/events.js b/node_modules/socket.io-client/lib/events.js
--- a/node_modules/socket.io-client/lib/events.js
+++ b/node_modules/socket.io-client/lib/events.js
@@ -178,5 +178,5 @@
 
 })(
     'undefined' != typeof io ? io : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('./io')
 );
diff --git a/node_modules/socket.io-client/lib/io.js b/node_modules/socket.io-client/lib/io.js
--- a/node_modules/socket.io-client/lib/io.js
+++ b/node_modules/socket.io-client/lib/io.js
@@ -126,9 +126,8 @@
 
     io.Transport.XHR = require('./transports/xhr').XHR;
 
-    io.transports.forEach(function (t) {
-      io.Transport[t] = require('./transports/' + t)[t];
-    });
+    io.Transport.websocket = require('./transports/websocket').websocket;
+    io.Transport['xhr-polling'] = require('./transports/xhr-polling')['xhr-polling'];
 
     /**
      * Expose Socket
diff --git a/node_modules/socket.io-client/lib/namespace.js b/node_modules/socket.io-client/lib/namespace.js
--- a/node_modules/socket.io-client/lib/namespace.js
+++ b/node_modules/socket.io-client/lib/namespace.js
@@ -238,5 +238,5 @@
 
 })(
     'undefined' != typeof io ? io : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('./io')
 );
diff --git a/node_modules/socket.io-client/lib/parser.js b/node_modules/socket.io-client/lib/parser.js
--- a/node_modules/socket.io-client/lib/parser.js
+++ b/node_modules/socket.io-client/lib/parser.js
@@ -258,5 +258,5 @@
 
 })(
     'undefined' != typeof io ? io : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('./io')
 );
diff --git a/node_modules/socket.io-client/lib/transport.js b/node_modules/socket.io-client/lib/transport.js
--- a/node_modules/socket.io-client/lib/transport.js
+++ b/node_modules/socket.io-client/lib/transport.js
@@ -252,5 +252,5 @@
   };
 })(
     'undefined' != typeof io ? io : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('./io')
 );
diff --git a/node_modules/socket.io-client/lib/transports/flashsocket.js b/node_modules/socket.io-client/lib/transports/flashsocket.js
--- a/node_modules/socket.io-client/lib/transports/flashsocket.js
+++ b/node_modules/socket.io-client/lib/transports/flashsocket.js
@@ -187,5 +187,5 @@
   io.transports.push('flashsocket');
 })(
     'undefined' != typeof io ? io.Transport : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('../io')
 );
diff --git a/node_modules/socket.io-client/lib/transports/htmlfile.js b/node_modules/socket.io-client/lib/transports/htmlfile.js
--- a/node_modules/socket.io-client/lib/transports/htmlfile.js
+++ b/node_modules/socket.io-client/lib/transports/htmlfile.js
@@ -169,5 +169,5 @@
 
 })(
     'undefined' != typeof io ? io.Transport : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('../io')
 );
diff --git a/node_modules/socket.io-client/lib/transports/jsonp-polling.js b/node_modules/socket.io-client/lib/transports/jsonp-polling.js
--- a/node_modules/socket.io-client/lib/transports/jsonp-polling.js
+++ b/node_modules/socket.io-client/lib/transports/jsonp-polling.js
@@ -251,6 +251,6 @@
 
 })(
     'undefined' != typeof io ? io.Transport : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('../io')
   , this
 );
diff --git a/node_modules/socket.io-client/lib/transports/websocket.js b/node_modules/socket.io-client/lib/transports/websocket.js
--- a/node_modules/socket.io-client/lib/transports/websocket.js
+++ b/node_modules/socket.io-client/lib/transports/websocket.js
@@ -192,6 +192,6 @@
 
 })(
     'undefined' != typeof io ? io.Transport : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('../io')
   , this
 );
diff --git a/node_modules/socket.io-client/lib/transports/xhr-polling.js b/node_modules/socket.io-client/lib/transports/xhr-polling.js
--- a/node_modules/socket.io-client/lib/transports/xhr-polling.js
+++ b/node_modules/socket.io-client/lib/transports/xhr-polling.js
@@ -172,6 +172,6 @@
 
 })(
     'undefined' != typeof io ? io.Transport : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('../io')
   , this
 );
diff --git a/node_modules/socket.io-client/lib/transports/xhr.js b/node_modules/socket.io-client/lib/transports/xhr.js
--- a/node_modules/socket.io-client/lib/transports/xhr.js
+++ b/node_modules/socket.io-client/lib/transports/xhr.js
@@ -212,6 +212,6 @@
 
 })(
     'undefined' != typeof io ? io.Transport : module.exports
-  , 'undefined' != typeof io ? io : module.parent.exports
+  , 'undefined' != typeof io ? io : require('../io')
   , this
 );
